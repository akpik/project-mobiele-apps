<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="TestProjectMobiele.Views.LogInPage"
             BackgroundColor="SteelBlue"
             Title="Log In">

    <StackLayout Margin="20" HorizontalOptions="CenterAndExpand" VerticalOptions="Center">
        <Label TextColor="White" FontSize="Large" Text="Wachtwoord:" Margin="5" HorizontalOptions="Center"/>
        <Entry FontSize="Large" HorizontalTextAlignment="Center" Text="{Binding TekstVak}" TextColor="LightGray" Placeholder="vul hier je code in" Margin="20"/>
        <Button Text="Log In" Command="{Binding LoginKnopGedrukt}" TextColor="White"/>

    </StackLayout>
</ContentPage>